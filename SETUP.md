# Оригинальный Look + бот

## Текущая рабочая конфигурация

Перенос завершён: исходный репозиторий `scvrdio/look`, Vercel-проект
`look` (`prj_1fVYqaZ3M1RIxGfsJY99aGa8IaAo`) и адрес
`https://look-green.vercel.app`. Это не переименование `look-notify`.
Webhook: `https://look-green.vercel.app/api/telegram`. Общее меню и личное меню
владельца тоже открывают look-green. Расписание Look включено; расписания
look-notify и scvrdio выключены. Эти старые проекты оставлены только для отката.
Не удалять Supabase `aarbxzaofhpreyrqqyso`: обе версии используют те же подписки.
`MINI_APP_URL` задаёт адрес кнопок; по умолчанию теперь исходный look-green.
Серверные ключи заданы в Production исходного проекта. Не удалять Supabase.

Код `9763b27` уже опубликован в `scvrdio/look/main` и на look-green:
production `dpl_3HDMivEq9h2bPvjGhHp5JvJmm3Ag`, READY.
После исправления ключа проверены HTTP 200 входа и библиотеки: все девять
исходных сериалов сохранены; с новым «Привет, Андрей!» всего десять.
Dry-run уведомлений: checked=10, failed=0, sent=0, pending=1.
Тест /start отправил меню. Неавторизованные series/check/telegram возвращают 401.
Ошибок в логах проверенного деплоя не обнаружено. Фактический следующий запуск
по расписанию и открытие обеих кнопок на телефоне отдельно не наблюдались.

Локальная привязка `.vercel/project.json` и Git remote `origin` уже указывают
на исходный look. Старый remote сохранён под именем `notify-archive`.
Проверки: `scripts/restore-original.mjs` (readiness, verify, connection).
Одноразовые dry-run/connect/test-menu требуют секретов перехода; временный
`.local-data/original-cutover.json` после проверки удалён. Не запускать configure
повторно без намерения заменить секрет webhook и перенастроить Telegram.

Ниже — состояние предыдущего этапа, до возврата в исходный проект.

Ветка `codex/look-original-backend` основана на оригинальном `look-main`.
Основной интерфейс, шторки и анимации взяты из исходного приложения.
Prisma заменена серверным доступом к Supabase REST; каталог — TVmaze, как у бота.

## Локальный предпросмотр

`pnpm install --frozen-lockfile`, затем `pnpm dev`.
Открыть http://localhost:3000.

Без настроек Supabase в development используются два демонстрационных сериала
с сокращённым набором эпизодов и без постеров. Отметки сохраняются в
`.local-data/1.json`; эта папка не попадает в Git. Это НЕ боевой аккаунт Telegram.
Docker, PostgreSQL и Prisma для предпросмотра не нужны.

## Подключение к настоящему боту

1. Выполнить `supabase/look-progress.sql` в том же проекте Supabase, где бот
   хранит таблицу `subscriptions`. Скрипт добавляет две отдельные таблицы;
   существующие подписки и курсор уведомлений не изменяет.
2. Задать только на сервере `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`,
   `TELEGRAM_BOT_TOKEN` (токен именно того бота, который открывает миниапп).
   Рекомендуется также отдельный случайный `SESSION_SECRET`.
   Не использовать префикс NEXT_PUBLIC для этих ключей и не коммитить `.env.local`.
3. Собрать `pnpm build`, развернуть по HTTPS и установить URL в кнопке
   web_app/меню бота. Вход проверяется прямо сервером Look; BOT_API_URL не нужен.
4. Проверить на реальном аккаунте: подписка в боте видна в миниаппе, новая
   подписка из миниаппа видна в боте, отметки эпизодов сохраняются после открытия.
   Прогресс просмотра не меняет `last_episode_id`, которым пользуются уведомления.

Команды Telegram и проверка новых серий теперь реализованы в этом же приложении:
`POST /api/telegram` и `GET /api/check`. У них единая таблица `subscriptions`
с миниаппкой. Для production нужны также два разных случайных серверных секрета:
`TELEGRAM_WEBHOOK_SECRET` и `CRON_SECRET`. Первый передаётся в Telegram setWebhook,
второй Vercel автоматически прикладывает к запросу расписания как Bearer token.
`vercel.json` задаёт ежедневную проверку в 09:00 UTC. На Hobby запуск возможен
в пределах часового окна, а не строго в указанную минуту.

Для проверки без отправки сообщений и изменения курсоров используется
`GET /api/check?dryRun=1` с тем же секретом расписания.
`pnpm test:bot` проверяет команды, поиск, обработку ошибок отправки и авторизацию.
`pnpm build` проверяет production-сборку.

Уведомления отмечаются отправленными только после подтверждения Telegram.
Гарантии exactly-once нет: обрыв между отправкой и записью курсора или параллельные
ручная и автоматическая проверки могут привести к повторному уведомлению.
Старый планировщик нужно отключить перед завершением переключения.
В ходе текущего переключения он отключён: Cron Jobs проекта `scvrdio` — Disabled.
Расписание проекта `look-notify` активно.

Пауза в Look — папка просмотра, а не отключение уведомлений бота.
Ручное изменение каталожных сезонов не поддерживается новым сервером;
основной сценарий — добавление сериалов через поиск TVmaze.

## Состояние данных и интерфейса

SQL-миграция применена.
Серверная проверка входа с токеном `@wellook_bot` и чтения девяти существующих
подписок прошла; анонимный запрос библиотеки возвращает 401.
Окончательная проверка открытия внутри Telegram выполняется пользователем.

Целевой Telegram-бот — только `@wellook_bot`. Vercel-проект `look-notify`
содержит интерфейс, команды и уведомления. Старый `scvrdio` сохраняется для
отката, но после переключения не должен обслуживать webhook или расписание.
Supabase остаётся прежним: `aarbxzaofhpreyrqqyso`.
Адрес приложения: https://look-notify.vercel.app.
Старую основную ветку `main` пока не публиковать повторно: актуальный исходный
код находится в `codex/look-original-backend`.

Подписки не пересоздавались, курсоры уведомлений не сбрасывались.

Подписки без отметок о просмотре отображаются в папке «Буду смотреть».
В «Смотрю сейчас» они переходят после начала просмотра. Старые отметки просмотра
из Prisma не переносились; это отдельные данные, не курсоры уведомлений бота.

## Предыдущий переход (история, не текущая конфигурация)

Переключение на единый Look выполнено через временный закрытый маршрут.
Telegram подтвердил webhook `https://look-notify.vercel.app/api/telegram`,
очередь ожидающих обновлений была пустой. Обработчик `/start` отправил меню.
Production dry-run вернул `checked: 9, failed: 0, sent: 0, pending: 1`.
Временный маршрут настройки удалён из итогового кода.
Временные переменные `TELEGRAM_SETUP_SECRET` и `TELEGRAM_SETUP_EXPIRES` удалены.
Сборка переключения: `873f2cc`; итоговый runtime без временного маршрута: `32596f2`.

До объединения webhook `@wellook_bot` был на `https://scvrdio.vercel.app/api/telegram`
с новым секретом. Настроены команды и постоянная кнопка миниаппки, включая
индивидуальное меню владельца. Тест `/start` через обработчик завершился HTTP 200
и отправил меню владельцу. Проверены HTTP 200 с секретом и 401 без секрета.
Предыдущий секрет webhook больше не принимается; старый `@scvrdiobot` не является
точкой входа. Его регистрация webhook в Telegram не удалялась (старый токен
не извлекался); обработчик отклоняет старые запросы.

Деплои для отката к разделённой архитектуре:
- Look: `dpl_E44WktAw3PxRXTBKKFhgwXGND4xZ`.
- Команды/уведомления: `dpl_BgMQQQ5MP7ma7AKjMP6KwvE3iQPy`.

`scripts/wellook-switch.mjs` — ручной операторский инструмент для этого перехода,
не часть runtime. Он требует исключённый из Git `.env.telegram-switch.local`.
После перехода локальный файл с токеном удалён; не восстанавливать его без необходимости.
